package defpackage;

import android.content.Context;
import android.net.Uri;
import android.os.ParcelFileDescriptor;

/* compiled from: FileDescriptorResourceLoader */
public class na extends mt<ParcelFileDescriptor> implements mz<Integer> {
    public na(Context context, mr<Uri, ParcelFileDescriptor> uriLoader) {
        super(context, (mr) uriLoader);
    }
}
