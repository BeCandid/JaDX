package defpackage;

import android.content.Context;
import android.net.Uri;
import java.io.InputStream;

/* compiled from: StreamResourceLoader */
public class nh extends mt<InputStream> implements ng<Integer> {
    public nh(Context context, mr<Uri, InputStream> uriLoader) {
        super(context, (mr) uriLoader);
    }
}
